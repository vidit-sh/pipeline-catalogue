{{#selectedItems}}
{{#isStageVisible}}
{{#libraries}}
library identifier '{{& .}}'
{{/libraries}}
{{/isStageVisible}}
{{/selectedItems}}

pipeline {
    agent any
 
    stages {
		{{#selectedItems}}
        {{#isStageVisible}}
        stage('{{& name}}') {
            steps {
                {{#commands}}
                {{& .}}
                {{/commands}}
            }
        }
        {{/isStageVisible}}
		{{/selectedItems}}
    }
 }