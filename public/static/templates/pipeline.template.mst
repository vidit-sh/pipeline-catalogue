{{#selectedItems}}
{{#isStageVisible}}
{{#libraries}}
library identifier '{{& .}}'
{{/libraries}}
{{/isStageVisible}}
{{/selectedItems}}

pipeline {
    agent any
 
    stages {
		{{#selectedItems}}
        {{#isStageVisible}}
        stage('{{& name}}') {
            steps {
                script {
                    {{#commands}}
                    {{& .}}
                    {{/commands}}
                }
            }
        }
        {{/isStageVisible}}
		{{/selectedItems}}
    }
 }